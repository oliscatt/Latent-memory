# 安全策略

Latent 处理的是人格文件、长期记忆和会话线索。安全问题里可能天然带着私人内容；报告漏洞时，
先保护内容，再追求复现完整。

## 支持范围

首个正式版本发布前，只维护 GitHub 默认分支 `main` 与正在验收的发布候选。正式版本发布后，
安全修复优先落在最新版本；旧提交只做力所能及的判断，不承诺回补。

报告时请附 tag 或完整 commit。没有版本条件的现象无法判断是否仍存在。

初期不承诺响应时间。数据损坏或不可恢复风险、跨用户读取／未授权访问和凭证问题优先处理；
普通接入失败、没有浮现或回答质量问题排在其后。每条报告都会被阅读和回复，修复或迭代完成后
会回到原报告说明结果。

## 如何私下报告

不要在公开 issue、Discussion、提交或 Pull Request 中写漏洞细节。请打开仓库的
**Security → Advisories → Report a vulnerability**，通过 GitHub 私密漏洞报告提交；标题只写
影响范围，不放利用细节、token、路径或记忆原文。

即使使用私密漏洞报告，也不要上传真实人格文件、记忆库、导出包、token 或带私人内容的截图。
需要夹具时，请先用完全虚构的数据复现。

报告至少包括：

- 受影响的 tag／commit、Python 与操作系统；
- stdio 或 HTTP，绑定地址是否为回环，是否经过 TLS 反代；
- 最小虚构复现步骤、实际结果、预期结果与影响；
- 是否可能读取、修改或泄露不属于调用者的记忆；
- 已经采取的临时缓解措施。

普通检索漏召回、模型没有主动调用工具、或回答质量不理想，一般是产品缺陷而非安全漏洞；
若它同时造成跨用户读取、未授权写入、凭证泄露或任意文件访问，则按安全问题私下报告。

自动浮现异常的第一动作是关闭该可选能力；主动检索、换窗召回与正常写回不受影响。自动浮现
检索路径只读记忆库，不改正文、索引、撤回／变迁账本或权重；如果现象显示这些数据被修改，
请按数据损坏风险优先报告。历史保留模式把错误片段写入对话历史不等于记忆库损坏，但仍应停止
新增注入，并由宿主移除旧资料或开启不继承旧片段的新会话。

## 当前安全边界

- 默认 stdio 与回环 HTTP 信任同一台机器上的进程；它们不是多租户隔离边界。
- `--http` 提供的是裸 HTTP。绑定非回环地址必须配置 token，但 token 与返回的记忆正文仍是
  明文传输；公网部署应把服务绑回环，由反向代理负责 HTTPS、访问控制和限流。
- Bearer token 只应通过 `--token` 或 `MEMORY_HTTP_TOKEN` 提供，不要写进仓库、人格文件、
  issue 或日志。
- 云端 embedding 会把语料块和查询发送给所选服务商；本地 embedding 与零依赖档不发送这些
  内容。无论使用哪种检索档，召回结果一旦返回聊天客户端，就会进入该客户端／模型的上下文。
- `latent_supersede` 把当时真实、后来变化的旧事实保留在 `.supersessions.json` 关系链中，
  默认检索隐藏但历史查询仍会返回；它不是纠错或删除。
- `latent_correct` 是“从未真实过”的逻辑纠错与撤回，不是安全删除；`latent_cleanup` 会把目标
  移出活动语料，但仍保留可恢复隔离副本。这些操作都不清除 Git 历史、客户端记录或第三方日志。

完整数据流、落盘文件和删除边界见 [`docs/隐私与数据流.md`](docs/隐私与数据流.md)。

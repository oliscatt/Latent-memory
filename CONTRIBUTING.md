# 参与贡献

谢谢你愿意把实测结果或改动交回来。这个项目的用户资料高度私密；提交 Issue 或 Pull Request
前，请先确认内容里没有真实人格、记忆正文、问句、回答、token、私人路径或原始导出包。

## 当前最需要的 Pull Request

- **兼容性记录**：写清宿主名称与版本、hook 实际执行点、模式、Latent 完整 commit、判据与
  采集条件；成功和失败都需要。
- **宿主 hook 适配示例**：把自动浮现放在用户消息提交后、首个模型请求前，并正确实现临时或
  历史保留模式、关闭路径和部署报告。

兼容性结果分成“维护者复现通过”“外部实测，维护者未复现”“机制／文档推断”三档。外部记录
不会因为一次跑通就自动变成受支持宿主，但会如实保留并帮助下一轮复现。

## 自动浮现的硬红线

**不接受把隐藏入口 `latent_passive_recall` 暴露成普通模型工具的改动。** 它必须由宿主 hook
调用，并从聊天模型可见的工具列表移除；否则会绕过用户选择、时序、历史模式、幂等、预算和
失效协调。

自动浮现默认关闭。任何适配都必须先确认宿主具备合格 hook 时机、本轮输入能力和历史控制能力，
再由用户明确选择是否开启；不得用“先试试看”代替能力证明或用户授权。

## 提交前

1. 从最新 `main` 建分支，说明改动解决什么具体问题。
2. 文档、commit message 与代码注释使用中文，代码标识符除外。
3. 示例只用完全虚构的数据；安全问题不要开公开 Issue，按 `SECURITY.md` 私下报告。
4. 运行 `python tests/run_release_checks.py`。宿主适配另附最小可复跑夹具和部署报告。
5. 数字结论同时写判据和采集条件；不要用“模型答得像记得”代替实际工具或注入证据。

Pull Request 里请链接对应 Issue（如有），并说明：实际结果、预期结果、验证范围、未验证范围和
回滚方法。不要把某个相邻宿主或模式的结果借给本次改动。

